<?php
$help =<<<'HELPTEXT'
【STEP1】

今から、installコマンドを実行します。
読み取り元のファイル(composer.lockファイル)は  `/opt/work/composer.lock` です。
プログラムから読み込む時は、ファイルパスを定数 `LOCK_FILE` で参照できます。
(気になる方は、 `var_dump(LOCK_FILE);` を実行して見てください)

# TODO
`composer.lock` の中身を連想配列として読み取ってください。
その結果を変数 `$lockData` に代入してください。
HELPTEXT;

/* === STEP-1 ココから === */
// $lockData = ・・・

/* === STEP-1 ココまで === */
